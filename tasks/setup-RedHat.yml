---
- name: Ensure Apache is installed on RHEL.
  yum:
    name: "{{ item }}"
    state: installed
    enablerepo: "{{ apache_enablerepo }}"
    disablerepo: "{{ apache_disablerepo }}"
  with_items: "{{ apache_packages }}"
